# Output Contract

Shared contract file: `output-contract.schema.json`

Supported result types:
- `artifact`
- `decision_request`
- `escalation_request`
- `explicit_failure`

Rule:
- Every official stage agent must return exactly one contract-compliant JSON object.
